package sde

model SDE {
  
  param Params params
  param Integer N
  
  param Plate<String> myPlate
  param Plated<RealVar> myPlated ?: Plated::latent("myPlated") [null]
  
  random List<RealVar> d ?: latentRealList(N)
  
  laws {
    
    for (Integer i : 0 ..< N) {
      
      //d.get(i) 
      
    }
    
  }
}