package ptgrad

model Conjugate {
  
  random RealVar mean ?: latentReal
  random RealVar observation ?: 3
  
  laws {
    
    mean ~ Variational()
    
    observation | mean ~ Normal(mean, 1.0)
    
  }
}